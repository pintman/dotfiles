## Agent skills

### Issue tracker

Issues leben als GitHub Issues in diesem Repo (pintman/dotfiles), über die `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` auf Root-Ebene. See `docs/agents/domain.md`.
